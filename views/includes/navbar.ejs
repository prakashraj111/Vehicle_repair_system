<nav class="bg-red-900 shadow-md">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      
      <!-- Logo Section -->
      <div class="flex-shrink-0">
        <a href="/posts" class="text-xl font-bold text-gray-100">
          <i class="fas fa-home"></i>
        </a>
      </div>

      <!-- Main Navigation Links -->
      <div class="hidden md:flex items-center space-x-8">
        <!-- Search Section -->
        <form method="post" action="/posts/search" class="flex items-center space-x-2">
          <select 
            id="options" 
            name="options" 
            class="bg-white text-gray-700 px-4 py-2 rounded border focus:outline-none"
          >
            <option disabled selected>Select Category</option>
            <option value="Bike">Bike 🏍️</option>
            <option value="Bus">Bus 🚌</option>
            <option value="Bicycle">Bicycle 🚲</option>
          </select>
          <input 
            type="text" 
            placeholder="Search by place name"
            id="search"
            name="search" 
            class="bg-white text-gray-700 px-4 py-2 rounded border focus:outline-none"
          />
          <button 
            type="submit" 
            class="bg-black text-white px-4 py-2 rounded hover:bg-gray-700 focus:outline-none"
          >
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </form>

        <!-- Profile Dropdown -->
        <div class="relative group">
          <button class="flex items-center space-x-2 focus:outline-none">
           <% if(currUser) { %>
            <span class="text-gray-100"><a href="/myaccount">My Profile</a></span>
            <% } %> 
            <% if(!currUser) { %>
              <span class="text-gray-100 mr-3"><a href="/login">Log in</a></span>
              <span class="text-gray-100"><a href="/signup">Sign up</a></span>
            <% } %>
          </button>
        </div>
      </div>

      <!-- Mobile Menu Button -->
      <div class="flex md:hidden">
        <button id="menu-btn" class="text-gray-100 focus:outline-none">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu Dropdown -->
  <div id="mobile-menu" class="hidden md:hidden">
    <div class="px-4 pt-2 pb-3 space-y-1">
      <!-- Search Section -->
      <form method="post" action="/posts/search" class="space-y-2">
        <select id="options2" name="options" class="w-full px-3 py-2 text-gray-700 rounded border focus:outline-none">
          <option disabled selected>Select Category</option>
          <option value="Bike">Bike 🏍️</option>
          <option value="Bus">Bus 🚌</option>
          <option value="Bicycle">Bicycle 🚲</option>
        </select>
        <input 
          type="text" 
          placeholder="Search..." 
          id="search2" 
          name="search"
          class="w-full px-3 py-2 rounded border focus:outline-none"
        />
        <button 
          type="submit" 
          class="w-full bg-black text-white px-4 py-2 rounded hover:bg-gray-700 focus:outline-none"
        >
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>

      <!-- Profile Link -->
      <% if(currUser) { %>
      <a href="/myaccount" class="block text-gray-100 hover:bg-gray-200 px-3 py-2 rounded">
        My Profile
      </a>
      <% } %> <% if(!currUser) { %>
      <a href="/login" class="block text-gray-100 hover:bg-gray-200 px-3 py-2 rounded">
        Log in
      </a>
      <% } %> 
    </div>
  </div>
</nav>
